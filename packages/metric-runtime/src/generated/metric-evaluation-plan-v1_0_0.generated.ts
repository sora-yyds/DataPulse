/* Deterministically generated by scripts/generate-artifacts.mjs; do not edit. */

/**
 * A single trusted metric identity and aggregate evaluated over selected accumulators.
 */
export interface MetricEvaluationPlan {
  schemaVersion: "1.0.0";
  metricId: string;
  aggregate: "COUNT_ROWS" | "SUM";
}
