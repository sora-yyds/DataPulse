# 自定义端点也必须提供模型发现

自定义模型端点除兼容 OpenAI Chat Completions 与浏览器 CORS 外，还必须提供鉴权后的 OpenAI-compatible `GET /models`；DataPulse AI 只允许从返回列表中选择模型并对所选模型执行合成连接测试，不提供手填模型 ID 或绕过入口。模型列表不可用、为空或格式不兼容时，该端点在 MVP 中不可使用。该决定收窄了对只实现补全接口的中转站与本地模型服务的兼容范围，但把“禁止错误模型 ID”的防错原则一致应用于全部供应商连接，并补充 ADR-0022 的自定义端点约束。
