# 故事蓝图使用版本化 JSON Schema 与永久迁移链

JSON Schema 是故事蓝图的唯一规范来源，每个本地项目、项目包和发布快照都保存 `schemaVersion`，兼容新增使用次版本、破坏性变化提升主版本；创作端保留从每个正式版本逐步迁移到当前版本的迁移器，迁移在副本上执行并验证，失败时保留原项目，观看端至少兼容当前和上一主版本。该选择增加长期迁移代码和历史样本维护成本，但保证本地长期项目与短期发布快照不会因应用升级失读，并由 CI 使用历史项目样本持续验证迁移与渲染。
