# 数值联动与文案重生成采用分层时延预算

分析条件变化后，界面须在 100 毫秒内响应，常见数据集的 KPI 与图表目标在 1 秒内更新、最大数据集不超过 2 秒，洞察须在 5 秒内通过本地叙事规则完成有效性判断并更新、隐藏或标记不适用；观看端不调用模型，项目所有者主动进行的 AI 文案增强采用异步有界超时，不承诺 5 秒成功。该约束要求确定性计算、叙事校验与生成式文案分层执行，以较复杂的状态管理换取可直接操控的编辑体验和内容一致性。
