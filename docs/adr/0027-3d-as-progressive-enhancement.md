# 3D 视觉场景只能作为渐进增强

受控渲染引擎必须先完成数据、图表、文案和交互的可读二维渲染，再延迟加载 3D 视觉场景；WebGL 不可用、GPU 性能不足、微信兼容性不佳、用户选择弱动效或持续掉帧时自动回退到 CSS/SVG，桌面以 60 FPS、移动端以 30 FPS 为目标。该选择限制 3D 成为必要叙事载体，以额外的双表现层测试换取首屏速度、跨设备可用性和任何降级情况下都不丢失信息。
