# 直接标识与所有者声明的机密字段采用同等保护

DataPulse AI 将原先模糊的“敏感字段”拆分为直接标识字段与机密字段：前者依据字段名和内容模式自动提示并允许所有者纠正，后者只能由所有者主动标记，系统不声称能够自动发现所有商业机密。两类字段默认都不得进入证据包；一旦选入发布明细，分享必须同时设置密码与有效期。未被标记的普通业务明细继续由分享者权衡，系统通过真实值预览、范围说明和二次确认支持知情决定，而不擅自阻止。该边界增加字段角色与发布校验状态，但避免把直接个人标识检测误述为全面的数据分类能力，并细化 ADR-0010 的保护范围。
