# 数据故事生成必须在有界时间内结束

常见档数据集必须同时满足不超过 50,000 行、1,500,000 个非空单元格、10 MB 输入文件且 `.xlsx` 解压内容不超过 100 MB，其完整可编辑草稿目标在 60 秒内生成；其余符合 ADR-0050 准入条件的边界档数据集目标在 3 分钟内完成。生成任务显示解析、洞察、故事与视觉阶段，超过 5 分钟必须明确失败并允许重试。该约束会限制无限制的分析搜索和视觉生成，但支持“10 分钟完成可分享成果”这一尚待真实用户验证的产品目标，并要求系统采用可观测、可终止的任务模型。
