# 数据故事跨数据集版本延续并在明确检查点固化

数据故事是跨多次数据更新持续存在的汇报实体；每次成功导入形成不可变的数据集版本，当前草稿绑定其中一个数据集版本并自动保存可撤销编辑，故事版本则把确切数据集版本与故事蓝图固化为不可变检查点，发布快照再由某个故事版本加密生成。故事版本在首次生成成功、所有者主动保存、更换数据前存在未固化修改、新数据生成完成以及每次发布前创建；普通标题、布局、时间范围和图表编辑只更新草稿与撤销历史，不为每次操作生成版本。该模型增加检查点与草稿分支管理，但既保证已发布结论和数据更新可复现，又避免细粒度编辑造成版本历史爆炸，并细化 ADR-0003。
