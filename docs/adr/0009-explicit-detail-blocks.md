# 允许显式发布选定明细，但不开放数据集

本地项目保存导入数据的具体值，但不把直接标识字段或机密字段放入证据包，也不交给 AI 自动发布；项目所有者可以选择特定字段与记录，在预览真实内容并二次确认后将其发布为明细区块，云端最多 24 小时保存这些明确发布的值。观看者只能获得明细区块明确包含的值，不能任意下钻、搜索或下载其余数据；受保护明细还必须遵守 ADR-0010 与 ADR-0049。该决定取代 ADR-0005 的绝对禁止，以受控暴露换取公司内部汇报所需的数据完整性。
