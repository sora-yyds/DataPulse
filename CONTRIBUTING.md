# 参与 DataPulse AI

感谢你参与 DataPulse AI。仓库仍处于 M0 工程底座阶段，贡献应优先关闭既定实施任务，不扩大已冻结的 MVP 范围。

## 开工前

1. 阅读 [AGENTS.md](AGENTS.md)，它定义仓库级安全、正确性、验证和文档同步规则。
2. 阅读 [CONTEXT.md](CONTEXT.md) 并沿用既有领域术语。
3. 在 [实施计划](docs/IMPLEMENTATION_PLAN.md) 中确认任务依赖，在 [M0 证据索引](docs/evidence/m0/evidence-index.json) 中确认 gate 状态。
4. 核对关联的 PRD 需求、当前有效 ADR、架构模块边界和里程碑退出标准。

如果事实源冲突，请先修正文档或新增明确取代关系的 ADR，不要在实现中暗自选边。

## 分支与提交

- 从最新 `main` 创建短期分支或使用 Fork；不要把未经检查的工作直接推送到受保护主干。
- 分支名应简短且可追踪，例如 `m0/story-schema-reader`、`fix/metric-merge`。
- 提交应保持单一目的并使用 Conventional Commits。标题和正文可以使用中文，例如：

```text
feat(schema): 增加故事蓝图有界读取器

在解析前限制输入字节和结构深度，并为未知版本返回稳定错误码。
补充合法、超限和恶意输入测试，失败时保留原始副本。
```

- 面向用户的变更在 M0-008 建立 Changesets 后必须附 Changeset；工具尚未落地前不要伪造命令或占位文件。
- 不重写他人的提交，不执行破坏性 Git 操作，不把真实秘密或用户数据写入历史。

## 修改原则

- 优先提交最小可运行纵向切片，避免只有目录、空脚本或无断言演示。
- 在公共边界解析、限尺寸并运行时校验所有外部输入；模型输出、文件、跨 Origin 消息和发布物都不可信。
- 修改 Schema、协议或跨包契约时，在同一变更中同步生产者、消费者、迁移、夹具和测试。
- UI 改动遵循 [DESIGN.md](DESIGN.md)，并以 WCAG 2.2 AA、键盘操作、弱动效和二维等价信息为硬门槛。
- 测试仅使用仓库合成数据或明确维护的本地语料。
- 新增依赖必须说明用途、浏览器／Worker 影响、包体成本、维护状态和许可证兼容性；第三方版权、许可证文本与 NOTICE 必须保留。

## 验证与交付

只运行并报告 [AGENTS.md 第 8 节](AGENTS.md#8-命令)中已经存在的精确命令。新增真实脚本时，同一变更必须更新该命令表和相关证据 gate；无法运行的检查应明确写出原因与风险。

提交或 PR 描述至少说明：

- 关联任务、需求与 ADR；
- 用户或工程结果；
- 已运行的命令及结果；
- 未运行项、外部阻塞和剩余风险；
- Schema、迁移、证据、视觉基线或文档是否需要同步。

## 安全问题

疑似漏洞、秘密泄露或数据边界绕过不要通过公开 Issue 披露。请遵循 [SECURITY.md](SECURITY.md)。

## 许可证

提交贡献即表示你有权提供该贡献，并同意其按仓库的 [`AGPL-3.0-only`](LICENSE) 许可证发布。
