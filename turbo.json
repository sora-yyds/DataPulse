{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "//#check:toolchain:root": {
      "cache": false,
      "outputs": []
    }
  }
}
