# DataPulse AI 安全策略

DataPulse AI 处理本地业务数据、模型凭据、浏览器密钥和临时加密发布包。请负责任地私密报告安全问题，不要在公开 Issue、讨论、PR、日志或测试夹具中披露漏洞细节、真实数据或秘密。

## 支持状态

项目尚未发布正式版本，目前没有受支持的生产版本或安全修复 SLA。`main` 是开发分支，不应部署为生产服务。首次正式发布时，本节将增加受支持版本窗口和修复策略。

## 私密报告

1. GitHub 私密漏洞报告截至 2026-08-04 尚未启用；启用后应优先使用仓库的 **Security → Advisories → Report a vulnerability** 入口。
2. 在私密报告入口启用前，请在 [公开 Issue](https://github.com/sora-yyds/DataPulse/issues/new) 中只请求维护者提供私密联系渠道，不要附带漏洞、PoC、密钥、用户数据、内部地址或可利用细节。
3. 若发现已泄露的凭据，请先在对应供应商撤销或轮换；报告中只提供已脱敏的凭据类型和影响范围，不粘贴秘密本身。

报告建议包含：

- 受影响的提交、版本、Origin、浏览器或部署方式；
- 可复现的最小步骤和预期安全属性；
- 已脱敏的影响说明；
- 是否涉及原始数据外发、模型凭据、设备密钥、加密协议、发布撤销、Connector 隔离、CSP/CORS 或 TTL 清理；
- 你建议的协调披露时间窗口。

只使用合成数据复现。不要访问、下载、修改或保留不属于你的数据，也不要进行拒绝服务、社会工程、供应链投毒或超出验证所需的扫描。

## 处理方式

维护者将在可用时确认报告、复核影响并协调修复与披露。由于项目尚未发布且没有商业支持承诺，本文件不承诺固定响应或修复时限；报告状态必须基于真实复核，不会把模拟结果表述为已完成外部认证。

修复不得通过降低数据驻留、密码学、Origin 隔离、无障碍或质量门槛完成。涉及协议或信任边界的修复必须同步 ADR、Schema、迁移、固定向量和恶意输入测试。

## 安全范围示例

- 原始文件、未发布记录、证据包或模型凭据越过既定边界；
- 项目包、发布包、Fragment、KDF、nonce/tag 或密钥包装实现缺陷；
- Viewer、Creator、API 或 Custom Connector 间的 Origin、Cookie、存储或消息隔离绕过；
- 模型、文件或发布物导致任意 HTML、JavaScript、CSS、Shader、SQL 或公式执行；
- 发布撤销、有效期、强制密码、对象生命周期或 TTL 清理绕过；
- 依赖、构建、Release、SBOM 或 GitHub Actions 供应链问题。

普通功能缺陷、使用问题和不含安全影响的性能问题可以使用公开 Issue。
