---
version: alpha
name: DataPulse AI
description: A trustworthy, precise and vivid design system for turning structured business data into editable, accessible data stories.

colors:
  primary: "#35D6F5"
  primary-hover: "#22B8D6"
  on-primary: "#07111F"
  primary-light: "#006C86"
  on-primary-light: "#FFFFFF"
  on-danger-dark: "#07111F"
  on-danger-light: "#FFFFFF"
  secondary: "#8B7CFF"
  secondary-light: "#5B45D6"
  accent: "#36D9A3"
  focus-ring: "#F4C361"
  focus-ring-light: "#5B45D6"
  canvas-dark: "#07111F"
  surface-dark: "#0D1A2B"
  surface-dark-raised: "#14243A"
  border-dark: "#263A55"
  control-border-dark: "#5D7290"
  text-dark: "#F3F8FF"
  text-dark-secondary: "#A7B7CC"
  text-dark-muted: "#7E91AA"
  canvas-light: "#F5F8FC"
  surface-light: "#FFFFFF"
  surface-light-raised: "#EDF3F9"
  border-light: "#CFDAE8"
  control-border-light: "#70839C"
  text-light: "#102033"
  text-light-secondary: "#4D6078"
  text-light-muted: "#5E7086"
  success-dark: "#36D9A3"
  warning-dark: "#F4C361"
  danger-dark: "#FF6B7E"
  success-light: "#087F5B"
  warning-light: "#A85D00"
  danger-light: "#B4233B"
  glass-canvas: "#EEF2FA"
  glass-surface: "#FFFFFF"
  glass-primary: "#5B45D6"
  glass-primary-hover: "#4933BC"
  glass-accent: "#007F9E"
  editorial-canvas: "#F5F0E6"
  editorial-surface: "#FFFDF8"
  editorial-text: "#1A1B1F"
  editorial-text-secondary: "#4D6078"
  editorial-text-muted: "#5B6C80"
  editorial-brand: "#9B2F48"
  editorial-primary: "#2457A7"
  editorial-primary-hover: "#193F7D"
  editorial-accent: "#6D4C74"
  enterprise-canvas: "#F5F7FA"
  enterprise-surface: "#FFFFFF"
  enterprise-text: "#172033"
  enterprise-primary: "#1F5FBF"
  enterprise-primary-hover: "#174A98"
  enterprise-accent: "#0E7490"
  chart-dark-1: "#35D6F5"
  chart-dark-2: "#8B7CFF"
  chart-dark-3: "#36D9A3"
  chart-dark-4: "#F4C361"
  chart-dark-5: "#FF6B7E"
  chart-dark-6: "#5A9CFF"
  chart-dark-7: "#E879F9"
  chart-dark-8: "#A8DF65"
  chart-light-1: "#007F9E"
  chart-light-2: "#5B45D6"
  chart-light-3: "#087F5B"
  chart-light-4: "#A85D00"
  chart-light-5: "#B4233B"
  chart-light-6: "#1769AA"
  chart-light-7: "#8B2C98"
  chart-light-8: "#527A12"
  sequential-dark-1: "#3E6578"
  sequential-dark-2: "#347E94"
  sequential-dark-3: "#2B98AE"
  sequential-dark-4: "#2EB6CD"
  sequential-dark-5: "#35D6F5"
  sequential-light-1: "#D6EEF3"
  sequential-light-2: "#9ED8E3"
  sequential-light-3: "#58BECE"
  sequential-light-4: "#168EAA"
  sequential-light-5: "#005E76"
  diverging-dark-negative: "#C084FC"
  diverging-dark-negative-soft: "#8B5FBF"
  diverging-dark-neutral: "#44536B"
  diverging-dark-positive-soft: "#2C8294"
  diverging-dark-positive: "#2EB6CD"
  diverging-light-negative: "#6D28D9"
  diverging-light-negative-soft: "#9B72D0"
  diverging-light-neutral: "#D7E0EA"
  diverging-light-positive-soft: "#3E91A4"
  diverging-light-positive: "#006C86"

typography:
  display-xl:
    fontFamily: 'Space Grotesk, Inter, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif'
    fontSize: 64px
    fontWeight: 700
    lineHeight: 1.05
    letterSpacing: -0.035em
  display-lg:
    fontFamily: 'Space Grotesk, Inter, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif'
    fontSize: 48px
    fontWeight: 700
    lineHeight: 1.1
    letterSpacing: -0.03em
  display-md:
    fontFamily: 'Space Grotesk, Inter, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif'
    fontSize: 36px
    fontWeight: 700
    lineHeight: 1.15
    letterSpacing: -0.025em
  heading-lg:
    fontFamily: 'Inter, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif'
    fontSize: 28px
    fontWeight: 700
    lineHeight: 1.25
    letterSpacing: -0.015em
  heading-md:
    fontFamily: 'Inter, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif'
    fontSize: 22px
    fontWeight: 700
    lineHeight: 1.35
    letterSpacing: -0.01em
  heading-sm:
    fontFamily: 'Inter, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif'
    fontSize: 18px
    fontWeight: 600
    lineHeight: 1.45
  body-lg:
    fontFamily: 'Inter, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif'
    fontSize: 18px
    fontWeight: 400
    lineHeight: 1.65
  body-md:
    fontFamily: 'Inter, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif'
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.625
  body-sm:
    fontFamily: 'Inter, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif'
    fontSize: 14px
    fontWeight: 400
    lineHeight: 1.55
  label:
    fontFamily: 'Inter, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif'
    fontSize: 13px
    fontWeight: 600
    lineHeight: 1.4
    letterSpacing: 0.01em
  caption:
    fontFamily: 'Inter, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif'
    fontSize: 12px
    fontWeight: 500
    lineHeight: 1.5
  metric-xl:
    fontFamily: 'Space Grotesk, Inter, "Noto Sans SC", sans-serif'
    fontSize: 56px
    fontWeight: 700
    lineHeight: 1
    letterSpacing: -0.035em
  metric-lg:
    fontFamily: 'Space Grotesk, Inter, "Noto Sans SC", sans-serif'
    fontSize: 40px
    fontWeight: 700
    lineHeight: 1.05
    letterSpacing: -0.025em
  axis:
    fontFamily: 'Inter, "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif'
    fontSize: 12px
    fontWeight: 500
    lineHeight: 1.4
  data-mono:
    fontFamily: '"IBM Plex Mono", "SFMono-Regular", Consolas, "Liberation Mono", monospace'
    fontSize: 13px
    fontWeight: 500
    lineHeight: 1.5

rounded:
  none: 0px
  control: 8px
  card: 16px
  panel: 20px
  hero: 28px
  pill: 9999px

spacing:
  none: 0px
  xxs: 2px
  xs: 4px
  sm: 8px
  md: 12px
  lg: 16px
  xl: 24px
  "2xl": 32px
  "3xl": 48px
  "4xl": 64px
  "5xl": 96px

components:
  button-primary:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.on-primary}"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "{spacing.md} {spacing.lg}"
  button-primary-hover:
    backgroundColor: "{colors.primary-hover}"
    textColor: "{colors.on-primary}"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "{spacing.md} {spacing.lg}"
  button-secondary:
    backgroundColor: "{colors.surface-dark-raised}"
    textColor: "{colors.text-dark}"
    borderColor: "{colors.control-border-dark}"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "{spacing.md} {spacing.lg}"
  button-danger:
    backgroundColor: "{colors.danger-dark}"
    textColor: "{colors.on-danger-dark}"
    typography: "{typography.label}"
    rounded: "{rounded.control}"
    padding: "{spacing.md} {spacing.lg}"
  input-default:
    backgroundColor: "{colors.surface-dark}"
    textColor: "{colors.text-dark}"
    borderColor: "{colors.control-border-dark}"
    typography: "{typography.body-sm}"
    rounded: "{rounded.control}"
    padding: "{spacing.md} {spacing.lg}"
  filter-chip:
    backgroundColor: "{colors.surface-dark-raised}"
    textColor: "{colors.text-dark-secondary}"
    borderColor: "{colors.control-border-dark}"
    typography: "{typography.label}"
    rounded: "{rounded.pill}"
    padding: "{spacing.sm} {spacing.md}"
  filter-chip-selected:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.on-primary}"
    typography: "{typography.label}"
    rounded: "{rounded.pill}"
    padding: "{spacing.sm} {spacing.md}"
  kpi-card:
    backgroundColor: "{colors.surface-dark-raised}"
    textColor: "{colors.text-dark}"
    borderColor: "{colors.border-dark}"
    typography: "{typography.metric-lg}"
    rounded: "{rounded.card}"
    padding: "{spacing.xl}"
  chart-frame:
    backgroundColor: "{colors.surface-dark}"
    textColor: "{colors.text-dark}"
    borderColor: "{colors.border-dark}"
    typography: "{typography.body-md}"
    rounded: "{rounded.card}"
    padding: "{spacing.xl}"
  tooltip:
    backgroundColor: "{colors.surface-dark-raised}"
    textColor: "{colors.text-dark}"
    borderColor: "{colors.control-border-dark}"
    typography: "{typography.body-sm}"
    rounded: "{rounded.control}"
    padding: "{spacing.md}"
  data-table:
    backgroundColor: "{colors.surface-dark}"
    textColor: "{colors.text-dark}"
    borderColor: "{colors.border-dark}"
    typography: "{typography.body-sm}"
    rounded: "{rounded.card}"
    padding: "{spacing.lg}"

# DataPulse extension: Google design.md preserves this unknown top-level key,
# while the repository checker resolves it into @datapulse/themes.
themes:
  deep-space-neon:
    label: 深空霓虹
    semanticColors:
      canvas: "{colors.canvas-dark}"
      surface: "{colors.surface-dark}"
      surface-raised: "{colors.surface-dark-raised}"
      text: "{colors.text-dark}"
      text-secondary: "{colors.text-dark-secondary}"
      text-muted: "{colors.text-dark-muted}"
      border: "{colors.border-dark}"
      control-border: "{colors.control-border-dark}"
      focus: "{colors.focus-ring}"
      primary: "{colors.primary}"
      primary-hover: "{colors.primary-hover}"
      on-primary: "{colors.on-primary}"
      on-danger: "{colors.on-danger-dark}"
      secondary: "{colors.secondary}"
      success: "{colors.success-dark}"
      warning: "{colors.warning-dark}"
      danger: "{colors.danger-dark}"
      chart-1: "{colors.chart-dark-1}"
      chart-2: "{colors.chart-dark-2}"
      chart-3: "{colors.chart-dark-3}"
      chart-4: "{colors.chart-dark-4}"
      chart-5: "{colors.chart-dark-5}"
      chart-6: "{colors.chart-dark-6}"
      chart-7: "{colors.chart-dark-7}"
      chart-8: "{colors.chart-dark-8}"
      sequential-1: "{colors.sequential-dark-1}"
      sequential-2: "{colors.sequential-dark-2}"
      sequential-3: "{colors.sequential-dark-3}"
      sequential-4: "{colors.sequential-dark-4}"
      sequential-5: "{colors.sequential-dark-5}"
      diverging-negative: "{colors.diverging-dark-negative}"
      diverging-negative-soft: "{colors.diverging-dark-negative-soft}"
      diverging-neutral: "{colors.diverging-dark-neutral}"
      diverging-positive-soft: "{colors.diverging-dark-positive-soft}"
      diverging-positive: "{colors.diverging-dark-positive}"
  soft-glass:
    label: 玻璃柔光
    semanticColors:
      canvas: "{colors.glass-canvas}"
      surface: "{colors.glass-surface}"
      surface-raised: "{colors.surface-light-raised}"
      text: "{colors.text-light}"
      text-secondary: "{colors.text-light-secondary}"
      text-muted: "{colors.text-light-muted}"
      border: "{colors.border-light}"
      control-border: "{colors.control-border-light}"
      focus: "{colors.focus-ring-light}"
      primary: "{colors.glass-primary}"
      primary-hover: "{colors.glass-primary-hover}"
      on-primary: "{colors.on-primary-light}"
      on-danger: "{colors.on-danger-light}"
      secondary: "{colors.glass-accent}"
      success: "{colors.success-light}"
      warning: "{colors.warning-light}"
      danger: "{colors.danger-light}"
      chart-1: "{colors.chart-light-1}"
      chart-2: "{colors.chart-light-2}"
      chart-3: "{colors.chart-light-3}"
      chart-4: "{colors.chart-light-4}"
      chart-5: "{colors.chart-light-5}"
      chart-6: "{colors.chart-light-6}"
      chart-7: "{colors.chart-light-7}"
      chart-8: "{colors.chart-light-8}"
      sequential-1: "{colors.sequential-light-1}"
      sequential-2: "{colors.sequential-light-2}"
      sequential-3: "{colors.sequential-light-3}"
      sequential-4: "{colors.sequential-light-4}"
      sequential-5: "{colors.sequential-light-5}"
      diverging-negative: "{colors.diverging-light-negative}"
      diverging-negative-soft: "{colors.diverging-light-negative-soft}"
      diverging-neutral: "{colors.diverging-light-neutral}"
      diverging-positive-soft: "{colors.diverging-light-positive-soft}"
      diverging-positive: "{colors.diverging-light-positive}"
  data-editorial:
    label: 数据编辑部
    semanticColors:
      canvas: "{colors.editorial-canvas}"
      surface: "{colors.editorial-surface}"
      surface-raised: "{colors.editorial-surface}"
      text: "{colors.editorial-text}"
      text-secondary: "{colors.editorial-text-secondary}"
      text-muted: "{colors.editorial-text-muted}"
      border: "{colors.border-light}"
      control-border: "{colors.control-border-light}"
      focus: "{colors.focus-ring-light}"
      primary: "{colors.editorial-primary}"
      primary-hover: "{colors.editorial-primary-hover}"
      on-primary: "{colors.on-primary-light}"
      on-danger: "{colors.on-danger-light}"
      secondary: "{colors.editorial-accent}"
      success: "{colors.success-light}"
      warning: "{colors.warning-light}"
      danger: "{colors.danger-light}"
      chart-1: "{colors.chart-light-1}"
      chart-2: "{colors.chart-light-2}"
      chart-3: "{colors.chart-light-3}"
      chart-4: "{colors.chart-light-4}"
      chart-5: "{colors.chart-light-5}"
      chart-6: "{colors.chart-light-6}"
      chart-7: "{colors.chart-light-7}"
      chart-8: "{colors.chart-light-8}"
      sequential-1: "{colors.sequential-light-1}"
      sequential-2: "{colors.sequential-light-2}"
      sequential-3: "{colors.sequential-light-3}"
      sequential-4: "{colors.sequential-light-4}"
      sequential-5: "{colors.sequential-light-5}"
      diverging-negative: "{colors.diverging-light-negative}"
      diverging-negative-soft: "{colors.diverging-light-negative-soft}"
      diverging-neutral: "{colors.diverging-light-neutral}"
      diverging-positive-soft: "{colors.diverging-light-positive-soft}"
      diverging-positive: "{colors.diverging-light-positive}"
  enterprise-minimal:
    label: 企业极简
    semanticColors:
      canvas: "{colors.enterprise-canvas}"
      surface: "{colors.enterprise-surface}"
      surface-raised: "{colors.surface-light-raised}"
      text: "{colors.enterprise-text}"
      text-secondary: "{colors.text-light-secondary}"
      text-muted: "{colors.text-light-muted}"
      border: "{colors.border-light}"
      control-border: "{colors.control-border-light}"
      focus: "{colors.focus-ring-light}"
      primary: "{colors.enterprise-primary}"
      primary-hover: "{colors.enterprise-primary-hover}"
      on-primary: "{colors.on-primary-light}"
      on-danger: "{colors.on-danger-light}"
      secondary: "{colors.enterprise-accent}"
      success: "{colors.success-light}"
      warning: "{colors.warning-light}"
      danger: "{colors.danger-light}"
      chart-1: "{colors.chart-light-1}"
      chart-2: "{colors.chart-light-2}"
      chart-3: "{colors.chart-light-3}"
      chart-4: "{colors.chart-light-4}"
      chart-5: "{colors.chart-light-5}"
      chart-6: "{colors.chart-light-6}"
      chart-7: "{colors.chart-light-7}"
      chart-8: "{colors.chart-light-8}"
      sequential-1: "{colors.sequential-light-1}"
      sequential-2: "{colors.sequential-light-2}"
      sequential-3: "{colors.sequential-light-3}"
      sequential-4: "{colors.sequential-light-4}"
      sequential-5: "{colors.sequential-light-5}"
      diverging-negative: "{colors.diverging-light-negative}"
      diverging-negative-soft: "{colors.diverging-light-negative-soft}"
      diverging-neutral: "{colors.diverging-light-neutral}"
      diverging-positive-soft: "{colors.diverging-light-positive-soft}"
      diverging-positive: "{colors.diverging-light-positive}"
---

# DESIGN.md — DataPulse AI 产品品牌与设计系统

本文件是 DataPulse AI 品牌视觉、界面语言和数据可视化的唯一手写设计事实源。YAML front matter 中的 Token 是精确规范值；正文解释这些值的角色、使用边界与设计理由。若两者出现数值歧义，以 Token 为准，并在同一变更中修正文案。

`DESIGN.md` 不能覆盖 [PRD](docs/PRD.md)、[架构](docs/ARCHITECTURE.md)、当前有效 ADR 或 WCAG 发布门槛。实现、Storybook、CSS 变量、ECharts 主题和静态导出样式必须由本文件派生或与其同步，不得形成第二套手写品牌系统。

## Overview

### 品牌命题

**品牌主张：让数据脉动成可信故事。**

DataPulse AI 把一份静态业务表转化为可验证、可编辑、可分享的数据故事。品牌不卖“神奇 AI”，而是把确定性分析、清晰叙事和现代视觉组合成一种更快、更可信的汇报方式。

功能性说明优先使用：**“从 Excel/CSV 到可编辑、可分享的数据故事。”** 品牌主张用于首屏、发布物封面和社区传播，不能替代具体能力说明。

### 品牌人格

- **精确而不冰冷**：数字、口径和状态足够严谨，语言仍然自然、可理解。
- **鲜活而不炫技**：脉冲、景深和光感传达数据正在被理解，不制造赌场式霓虹或持续动画。
- **自信而不夸大**：告诉用户系统做了什么、依据什么、哪些仍未知；不使用“绝对安全”“一切洞察”等承诺。
- **高效而不仓促**：一键形成完成度高的初稿，同时让时间范围、口径、图表和文案都能直接编辑。
- **未来感而不科幻化**：现代材质和空间层次服务阅读，不能让界面看起来像虚构的控制室。

### 视觉签名

DataPulse 的识别来自三种稳定母题：

1. **脉冲轨迹**：由数据点和一条连续流线组成，表示“导入 → 分析 → 故事 → 分享”。它更接近数据趋势线，不画成医疗心电图。
2. **证据锚点**：关键数值、洞察和结论旁使用小型圆点/短线标记，连接口径、时间范围和证据入口。
3. **层次光场**：局部柔光、薄边框和克制景深让内容有空间感；数据层始终保持清晰、平整和可比较。

### 设计原则

1. **证据先于效果**：先看懂数值、单位、时间和结论，再感受到品牌氛围。
2. **故事而非控件墙**：默认成果是 4–8 个有阅读节奏的故事区块，不是同尺寸卡片铺满的大屏模板。
3. **改一处，全局一致**：编辑状态必须清楚展示影响范围；更新时不得把旧数值伪装成当前结果。
4. **直接操作优先**：核心编辑有可见控件，自然语言只是辅助。
5. **深度渐进增强**：二维内容先完整，3D、模糊和动效在能力允许时再增强。
6. **任何状态都可理解**：加载、空数据、小样本、受限草稿、失效结论和错误都有明确视觉与下一步。

## Colors

### 主品牌色

| Token | 角色 | 使用边界 |
|---|---|---|
| `{colors.primary}` | Pulse Cyan；深色界面的主要操作、选中状态、关键数据轨迹 | 以 `{colors.on-primary}` 承载文字；不用于大面积正文背景 |
| `{colors.primary-light}` | 浅色界面的链接、主要按钮与关键数据线 | 以 `{colors.on-primary-light}` 承载按钮文字 |
| `{colors.secondary}` / `{colors.secondary-light}` | Signal Violet；AI 建议、次级系列和叙事强调 | 不得成为所有渐变和所有卡片的默认装饰 |
| `{colors.accent}` | Insight Mint；成功、已验证、正向完成状态 | 业务增长并不自动等于“成功”，需由业务语义决定 |
| `{colors.focus-ring}` / `{colors.focus-ring-light}` | 深色/浅色表面的键盘焦点 | 只用于焦点，不与普通品牌强调或警告混用 |

主品牌渐变仅允许 `Pulse Cyan → Signal Violet`，用于英雄背景、脉冲轨迹或极少量强调描边。正文、关键数字和按钮文字不使用渐变填充。

### 中性与语义色

- 深色产品壳使用 `{colors.canvas-dark}`、`{colors.surface-dark}`、`{colors.surface-dark-raised}`、`{colors.border-dark}` 与三层深色文字 Token；必须靠边界辨认的控件使用 `{colors.control-border-dark}`。
- 浅色故事与导出使用 `{colors.canvas-light}`、`{colors.surface-light}`、`{colors.surface-light-raised}`、`{colors.border-light}` 与三层浅色文字 Token；必须靠边界辨认的控件使用 `{colors.control-border-light}`。
- 成功、警告、危险在明暗界面分别使用对应 `success-*`、`warning-*`、`danger-*` Token。状态必须同时提供图标和文字，颜色不是唯一信号。
- `{colors.text-light-muted}` 和 `{colors.text-dark-muted}` 仍须满足正文场景的可读性；更淡的颜色只能用于非文本装饰线。

### 四个故事主题

产品壳保持稳定品牌，故事画布可以选择四个受控主题。主题改变呈现气质，不改变数据语义、组件行为和可访问性。

| 主题 | 背景 / 表面 | 文字 | 主色 / 辅色 | 气质与适用场景 |
|---|---|---|---|---|
| 深空霓虹 | `{colors.canvas-dark}` / `{colors.surface-dark}` | `{colors.text-dark}` | `{colors.primary}` / `{colors.secondary}` | 默认展示型主题；科技、增长、数字业务。辉光只围绕焦点轨迹和少量重点 |
| 玻璃柔光 | `{colors.glass-canvas}` / `{colors.glass-surface}` | `{colors.text-light}` | `{colors.glass-primary}` / `{colors.glass-accent}` | 品牌、市场、消费业务。透明表面必须有可靠的纯色回退 |
| 数据编辑部 | `{colors.editorial-canvas}` / `{colors.editorial-surface}` | `{colors.editorial-text}` | `{colors.editorial-primary}` / `{colors.editorial-accent}` | 复盘、研究、管理层长文阅读。刊头可使用 `{colors.editorial-brand}` 与受控中文衬线体 |
| 企业极简 | `{colors.enterprise-canvas}` / `{colors.enterprise-surface}` | `{colors.enterprise-text}` | `{colors.enterprise-primary}` / `{colors.enterprise-accent}` | 财务、经营、正式内部汇报。低阴影、高对齐、强表格可读性 |

### 主题语义角色映射

组件不得直接引用某个主题的 primitive color。主题 Provider 是唯一允许把语义角色解析为 primitive Token 的位置；组件只消费下列稳定角色（实现为 `--dp-*` CSS 变量或等价类型化接口）。YAML `components` 是深空霓虹默认主题的解析结果，用于工具预览，不代表其他主题复用深色值。

| 语义角色 | 深空霓虹 | 玻璃柔光 | 数据编辑部 | 企业极简 |
|---|---|---|---|---|
| `--dp-canvas` | `{colors.canvas-dark}` | `{colors.glass-canvas}` | `{colors.editorial-canvas}` | `{colors.enterprise-canvas}` |
| `--dp-surface` | `{colors.surface-dark}` | `{colors.glass-surface}` | `{colors.editorial-surface}` | `{colors.enterprise-surface}` |
| `--dp-surface-raised` | `{colors.surface-dark-raised}` | `{colors.surface-light-raised}` | `{colors.editorial-surface}` | `{colors.surface-light-raised}` |
| `--dp-text` | `{colors.text-dark}` | `{colors.text-light}` | `{colors.editorial-text}` | `{colors.enterprise-text}` |
| `--dp-text-secondary` | `{colors.text-dark-secondary}` | `{colors.text-light-secondary}` | `{colors.editorial-text-secondary}` | `{colors.text-light-secondary}` |
| `--dp-text-muted` | `{colors.text-dark-muted}` | `{colors.text-light-muted}` | `{colors.editorial-text-muted}` | `{colors.text-light-muted}` |
| `--dp-border` | `{colors.border-dark}` | `{colors.border-light}` | `{colors.border-light}` | `{colors.border-light}` |
| `--dp-control-border` | `{colors.control-border-dark}` | `{colors.control-border-light}` | `{colors.control-border-light}` | `{colors.control-border-light}` |
| `--dp-focus` | `{colors.focus-ring}` | `{colors.focus-ring-light}` | `{colors.focus-ring-light}` | `{colors.focus-ring-light}` |
| `--dp-primary` | `{colors.primary}` | `{colors.glass-primary}` | `{colors.editorial-primary}` | `{colors.enterprise-primary}` |
| `--dp-primary-hover` | `{colors.primary-hover}` | `{colors.glass-primary-hover}` | `{colors.editorial-primary-hover}` | `{colors.enterprise-primary-hover}` |
| `--dp-on-primary` | `{colors.on-primary}` | `{colors.on-primary-light}` | `{colors.on-primary-light}` | `{colors.on-primary-light}` |
| `--dp-on-danger` | `{colors.on-danger-dark}` | `{colors.on-danger-light}` | `{colors.on-danger-light}` | `{colors.on-danger-light}` |
| `--dp-secondary` | `{colors.secondary}` | `{colors.glass-accent}` | `{colors.editorial-accent}` | `{colors.enterprise-accent}` |
| `--dp-success / warning / danger` | `success-dark / warning-dark / danger-dark` | `success-light / warning-light / danger-light` | `success-light / warning-light / danger-light` | `success-light / warning-light / danger-light` |
| `--dp-chart-1…8` | `{colors.chart-dark-1}` … `{colors.chart-dark-8}` | `{colors.chart-light-1}` … `{colors.chart-light-8}` | `{colors.chart-light-1}` … `{colors.chart-light-8}` | `{colors.chart-light-1}` … `{colors.chart-light-8}` |
| `--dp-sequential-1…5` | `{colors.sequential-dark-1}` … `{colors.sequential-dark-5}` | `{colors.sequential-light-1}` … `{colors.sequential-light-5}` | `{colors.sequential-light-1}` … `{colors.sequential-light-5}` | `{colors.sequential-light-1}` … `{colors.sequential-light-5}` |
| `--dp-diverging-*` | `diverging-dark-*` | `diverging-light-*` | `diverging-light-*` | `diverging-light-*` |

所有通用控件、KPI、Chart Frame、Tooltip 和 Data Table 都通过这组角色换肤。任何主题若缺少一个角色，构建或 Storybook 校验必须失败，不得回退到另一主题的具体颜色。数据编辑部的酒红 `{colors.editorial-brand}` 只用于刊头、标题标记和非交互叙事强调，不进入 `--dp-primary`、`--dp-secondary` 或状态角色；主操作使用蓝色 `{colors.editorial-primary}`，危险操作只使用 `danger-light`，两者不得以同一填充或文字处理呈现。

`--dp-secondary` 与 `--dp-success / warning / danger` 默认只用于数据图形、图标、边界和其他非文本信号，并且必须与所在表面达到 3:1；状态说明文字始终使用 `--dp-text`，以图标、形状和文案共同传达状态。普通叙事强调使用 `--dp-text` 加字重，交互链接使用通过 4.5:1 校验的 `--dp-primary` 并保留下划线或等价非颜色提示。唯一允许使用实心状态色承载文字的通用控件是 `button-danger`，且必须配对独立的 `--dp-on-danger`；不得复用 `--dp-on-primary`，也不得推断任意状态色都能直接充当普通前景文字或徽章底色。

### 用户品牌色

- 品牌预设只接受 Logo、一个品牌基色、明暗倾向和受控字体选择；不接受任意 CSS、字体文件或逐元素颜色。
- 用户品牌色先生成受控色阶，再选择满足对比度的文本、填充、边界和图表用途。不得直接把用户输入色用于正文或焦点环。
- 自动调整后同时展示“输入品牌色”和“实际应用色”；发布者不能绕过 WCAG 调整。
- 品牌色只改变强调与装饰，不改变成功/警告/危险、数据正负或隐私风险的语义；品牌派生不得修改 `danger-*` 或 `on-danger-*`。

## Typography

### 字体体系

- 拉丁字母与数字首选开源 `Space Grotesk`（展示和指标）与 `Inter`（界面和正文）；中文回退到受控、随产品打包的 `Noto Sans SC`，再使用系统中文无衬线回退。
- 数据编辑部主题的中文标题可以使用受控 `Noto Serif SC`；正文、控件、图表和数字仍使用无衬线体系。
- 字段名、证据 ID、代码和协议值使用 `{typography.data-mono}`，但不得把大段正文做成终端风格。
- 所有指标数字启用 lining/tabular numerals；货币符号、百分号、单位和小数位与主数值保持同一基线。
- 不使用超细字重；正文最低 400，标签最低 500，标题和指标通常 700。

### 层级

| 语义角色 | Token | 用法 |
|---|---|---|
| 故事英雄标题 | `{typography.display-xl}` / `{typography.display-lg}` | 每个故事最多一个；移动端使用响应式下调，不强行保持 64px |
| 区块标题 | `{typography.heading-lg}` / `{typography.heading-md}` | 说明“这个区块回答什么问题”，避免只有图表类型名 |
| 卡片标题 | `{typography.heading-sm}` | KPI、图表、说明和表格标题 |
| 正文 | `{typography.body-lg}` / `{typography.body-md}` / `{typography.body-sm}` | 结论、解释、辅助说明；中文行长优先 28–42 个全角字符 |
| 控件标签 | `{typography.label}` | 按钮、筛选器、Tab、字段角色和状态 Chip |
| 辅助信息 | `{typography.caption}` | 数据范围、更新时间、来源、脚注；不能承载关键警告 |
| KPI 数字 | `{typography.metric-xl}` / `{typography.metric-lg}` | 与标题、单位、比较基准组成完整语义，不单独展示裸数字 |
| 图表坐标 | `{typography.axis}` | 坐标轴、图例和短标签；空间不足时减少密度而不是无限缩小 |

标题采用紧凑字距，正文保持舒展行高。全大写只用于极短拉丁技术标签；简体中文不人为增加大字距来模拟“高级感”。

## Layout

### 空间系统

以 4px 为基础节拍，优先使用 front matter 的 `{spacing.*}` Token。相邻控件使用 `{spacing.sm}`–`{spacing.lg}`，卡片内边距使用 `{spacing.lg}`–`{spacing.xl}`，故事区块间距使用 `{spacing.2xl}`–`{spacing.4xl}`。不要创造 17px、23px 等一次性间距来修补布局。

### Creator 工作区

- 创作端正式面向桌面 Chrome/Edge。默认结构为顶部命令区、左侧项目/数据区、中央故事画布和右侧上下文检查器。
- 中央画布是视觉主角；侧栏不得同时抢占注意。窄桌面可以把一个侧栏收为抽屉，不能压缩故事到不可评审宽度。
- 直接编辑控件靠近被编辑对象；跨故事的时间范围、主题和发布状态保持全局可见。
- 对破坏性或外发操作使用独立确认层，不把发布、删除和普通样式按钮放在同一视觉权重。

### Viewer 与故事画布

- 故事最大内容宽度 1440px；正文叙事列通常 720–880px，图表全宽区块可扩展到内容容器。
- 桌面使用 12 列、平板 8 列、手机 4 列概念网格；实际 DOM 顺序始终保持线性可读。
- 允许全宽、分栏和重点区块，不允许自由像素坐标、重叠卡片、旋转面板或只能在固定分辨率成立的构图。
- 故事默认 4–8 个区块，交替使用结论、KPI、图表、解释和明细形成节奏；禁止把所有内容压成同高 KPI 宫格。

### 对齐和密度

- 数字右对齐、标签左对齐；同一比较组共享基线和小数位。
- 数据密度通过分组、层级和渐进披露管理，不靠极小字号或无限滚动横轴。
- 表格可以横向滚动，但标题、筛选状态和关键列需要保持可理解；页面主体不得出现横向滚动。

## Elevation & Depth

深度表达使用“表面色阶 → 1px 边框 → 阴影/辉光”的顺序，先用较轻手段建立层级。

- **Level 0**：页面/故事 Canvas，无阴影。
- **Level 1**：普通卡片，以边框和相邻表面色区分；最多使用柔和短阴影。
- **Level 2**：悬浮检查器、菜单、Tooltip 与确认层，可使用更明确阴影。
- **Level 3**：模态对话框和发布确认，必须配合遮罩、焦点锁定和清楚退出方式。
- 辉光只围绕品牌脉冲、选中数据或一次性关键强调；同一视口最多一个主要辉光焦点。
- `backdrop-filter` 只用于玻璃柔光的装饰层；文字和关键控件必须落在有足够对比的实色/近实色表面，并提供无模糊回退。
- 3D 场景永远位于数据内容之后，不遮挡坐标、标签、Tooltip、焦点环或点击目标。

## Shapes

- 控件使用 `{rounded.control}`，故事卡片使用 `{rounded.card}`，大型面板使用 `{rounded.panel}`，英雄视觉使用 `{rounded.hero}`。
- `{rounded.pill}` 只用于状态、筛选 Chip、短标签和分段控件；普通按钮、输入框和所有卡片不做胶囊化。
- 数据图形遵循其编码语义，不能为了品牌统一把柱、点和热力单元全部改成圆角胶囊。
- 图标采用 1.5–2px 线性几何语言，关键状态可增加小面积实心。避免混用拟物、表情符号和多套图标风格。
- 卡片轮廓保持平整；禁止随机斜切、过度有机气泡或影响比较的倾斜透视。

## Components

front matter 中的组件 Token 是深空霓虹默认解析样例。实现组件时，背景、文字、边界、焦点、状态和图表色必须从“主题语义角色映射”取得；不得把样例中的 `{colors.surface-dark}` 等 primitive 硬编码进组件。四主题共享结构、状态和可访问性，只由主题 Provider 解析角色。

### 通用控件

- `button-primary` 用于每个区域唯一的主要下一步；同一视口不要出现多个同权重高亮按钮。
- `button-secondary` 承担编辑、取消和次要操作；破坏性操作使用 `button-danger` 且需要清楚对象名称。
- `input-default`、Select、日期范围和搜索共享高度、标签、帮助文本、错误文本与焦点语言。占位符不能代替标签。
- `filter-chip` 表示可移除条件；选中态使用 `filter-chip-selected` 并同时显示勾选/移除图标。
- 键盘焦点在深色表面使用 `{colors.focus-ring}`、浅色表面使用 `{colors.focus-ring-light}`，均为 2px 可见轮廓和至少 2px offset；hover 不能是唯一交互反馈。

### 数据故事组件

- **KPI Card (`kpi-card`)**：必须包含指标名、格式化数值、单位、当前条件/周期、比较基准和证据入口。变化颜色之外还显示箭头与文本。
- **Chart Frame (`chart-frame`)**：包含问题式标题、范围/单位、绘图区、图例或直接标注、等价文字摘要和证据入口。标题不能只写“折线图”。
- **Tooltip (`tooltip`)**：按“维度 → 当前值与单位 → 对比/占比 → 数据状态”排序；支持键盘和触摸固定，不只依赖 hover。
- **Data Table (`data-table`)**：表头粘性、数值列右对齐、排序状态可见、空值与 0 明确区分。受保护明细在发布前显示保护状态。
- **时间范围控件**：同时显示起止、故事时区和当前数据截止点；编辑后明确提示所有依赖内容正在重算。
- **证据入口**：使用证据锚点母题，展开后显示口径、条件、样本、来源和限制，不用“AI 解释”替代事实。

### 系统状态组件

- 加载状态显示真实阶段：解析、洞察、故事、视觉。未知进度使用阶段指示，不伪造百分比。
- 空状态说明缺少什么、为什么需要以及下一步；不只展示插画。
- 错误状态包含发生了什么、数据是否保持安全、用户能做什么和可复制的稳定错误码。
- 受限草稿、小样本、失效结论和不适用状态采用中性/警告语义，不以红色“失败”污名化数据不足。
- AI 建议必须标记为建议并可撤销；确定性指标和用户已确认事实不使用闪光图标冒充 AI 能力。

## Do’s and Don’ts

### Do

- 让标题、时间范围、单位和结论在第一眼建立上下文。
- 用一个强视觉焦点和若干安静支撑区块形成故事节奏。
- 让所有数字可追溯，所有筛选状态可见，所有编辑影响可解释。
- 优先使用 Token、注册组件和受控主题；新模式先进入本文件和 Storybook。
- 为暗色、亮色、移动、键盘、触摸、弱动效和二维回退共同设计。
- 在真实业务语义不明确时使用中性颜色和措辞，而不是猜测好坏。

### Don’t

- 不要生成通用“紫蓝渐变 + 玻璃卡片 + 星光粒子”的 AI 模板界面。
- 不要让每张卡片发光、悬浮或播放循环动画。
- 不要用 3D 柱、3D 饼、透视面积或体积编码数据。
- 不要只靠红绿、透明度、hover 或动画表达差异。
- 不要隐藏 0、空值、小样本、数据截止点、筛选条件或结论失效。
- 不要在页面中硬编码未登记的颜色、字体、间距、圆角、阴影或缓动。
- 不要用“AI 驱动”“智能洞察”等空洞标签替代具体能力和依据。
- 不要为了截图效果牺牲滚动阅读、真实文本、键盘顺序和浏览器降级。

## Data Visualization & Information Design

### 图表职责

图表类型仍由 PRD 的适用规则和 Renderer 注册表决定；本文件只定义视觉表达，不能允许不兼容图表切换。首批支持标题/摘要、KPI、折线/面积、柱状/堆叠、环形、散点/气泡、热力、直方/箱线、矩形树图和明细表。

- 趋势优先折线；面积只在总量和基线含义明确时使用。
- 分类比较优先排序柱；分类默认 Top 8，其余显式合并“其他”并说明数量。
- 环形图只用于少量、互斥且合计有意义的组成关系；标签必须给出值/占比。
- 散点/气泡必须同时解释 x、y、大小和筛选；气泡面积而非半径对应数值。
- 堆叠图必须保证组成和总量语义成立；正负值或过多系列时改用更清楚的兼容图表。
- 直方/箱线用于分布，明确样本数、异常点和箱线统计定义。

### 图表配色

- 深色分类序列按 `{colors.chart-dark-1}` 至 `{colors.chart-dark-8}`；浅色序列按 `{colors.chart-light-1}` 至 `{colors.chart-light-8}`。序列顺序在同一故事中稳定。
- 第九类及以后不继续发明彩虹色；使用“其他”、分面、搜索/筛选或降低粒度。
- 顺序量严格使用当前主题的 `--dp-sequential-1…5`；发散量严格使用 `--dp-diverging-negative / negative-soft / neutral / positive-soft / positive`。每一侧从 `neutral → soft → endpoint` 的感知强度必须严格单调：深色主题的相对亮度递增，浅色主题的相对亮度递减，色度不得向端点逆向减弱；结果固定到视觉黄金样本。不得在 ECharts option 中临时插值出未登记色值。
- 通用发散色阶使用非状态语义的紫—青两端，只表示低于/高于中点或负/正方向，不解释为失败/成功、坏/好。若业务确实需要好坏发散，必须先有确定性业务判定规则，再另定义不与通用色阶别名的 `--dp-status-diverging-*` 角色，并在图例和文字中说明方向；不得把通用端点直接换成 `danger-*` / `success-*`。
- 热力图使用 5 档确定性分箱并显示完整图例、上下界与缺失值样式；连续视觉可以在这 5 个已登记 stop 间插值，但端点、色彩空间和分箱规则必须固定并进入视觉黄金样本。任一 data-cell stop 与相邻 `--dp-surface`、Canvas 或缺失态低于 3:1 时，深浅主题都必须以当前主题的 `--dp-control-border`、直接数据标签或图案提供非颜色边界，不靠填充单独证明单元存在；缺失值还必须使用独立图案或符号，不能只换成更浅填充。
- 成功/警告/危险色只编码明确业务状态，不作为任意分类序列。
- 颜色同时配合直接标签、形状、线型、图案或位置；灰度和常见色觉差异模拟下仍能区分。

### 坐标、数值与注释

- 默认不截断柱状图零基线；若分析理由要求截断，必须明显标记并在等价摘要中说明。
- 网格线退后，数据线和参考线前置；轴标题包含单位，Tooltip 不重复无意义小数。
- 0、缺失、未采集、不适用和被保护是五种不同状态，不能统一显示为 `-`。
- 日期、数字、货币默认使用中国区域格式，并固定使用故事时区。单位和显示尺度不改变底层值。
- 注释只标记可追溯事件、阈值或数据事实；AI 不得凭空生成原因标签。
- 每张图都提供短等价摘要；复杂图还提供可访问数据表或结构化明细入口。

## Interaction States

- hover 只增强发现，不承载唯一信息；键盘 focus、触摸 selected 和程序化 active 都有等价状态。
- 修改时间范围或条件后，控件在 100ms 内反馈选中状态；依赖 KPI/图表更新期间明确显示“正在更新”，不得把旧结论继续呈现为当前有效。
- Brush、Zoom、Legend toggle 和多选筛选必须显示当前范围、提供“一键重置”并进入撤销历史。
- 选择数据点时突出目标、降低其他元素但不使其不可读；焦点离开后恢复，不永久污染配色。
- 发布、真实明细外发、模型证据发送和删除属于高影响动作：先展示范围与后果，再要求明确确认。
- 取消、超时或错误后恢复到最后一致状态，保留用户输入并说明哪些步骤没有提交。

## Motion & 3D

Google alpha Token schema 尚未提供时间 Token；以下时长是本文件正文中的规范值，直到规范支持可迁移的 motion token：

| 类型 | 时长 | 用途 |
|---|---:|---|
| Instant | 0–80ms | 键盘焦点、按下反馈、无空间移动的状态切换 |
| Fast | 120ms | hover、Chip、图例、轻量颜色变化 |
| Standard | 180ms | 面板、Tooltip、筛选状态和小范围布局变化 |
| Emphasis | 280ms | 区块入场、数据状态切换、确认层 |
| Scene | 最长 480ms | 一次性英雄氛围和 3D 场景淡入；不阻塞内容 |

- 默认缓动：进入 `cubic-bezier(0.22, 1, 0.36, 1)`，退出 `cubic-bezier(0.4, 0, 1, 1)`；数据更新使用平稳、无过冲的 easing。
- 动效只由阅读进入、明确交互或数据变化触发。装饰性循环、视差追逐光标和无限漂浮默认禁止；加载动画必须功能性且可降级。
- 数字可以从旧的有效值过渡到新值，但最终值必须精确，过渡期间标记正在更新；不得从 0 伪造“增长”。
- `prefers-reduced-motion` 下移除空间移动、视差和计数动画，以即时替换或 ≤80ms 淡入维持状态连续性。
- 3D 延迟加载且在二维内容之后。桌面目标 60 FPS、移动目标 30 FPS；持续不达标、WebGL 不可用、微信兼容不佳或弱动效开启时自动回退 CSS/SVG。
- 3D 场景不接收原始数据，不创建第三套数据编码，不影响命中区域、键盘顺序、静态导出和屏幕阅读器内容。

## Responsive & Presentation Modes

### 断点与触摸

- Mobile：`<768px`，4 列概念网格，16px 页面 gutter，单列故事。
- Tablet：`768–1199px`，8 列概念网格，24px gutter；必要时分栏回落为单列。
- Desktop：`≥1200px`，12 列概念网格，32px gutter；超宽屏限制内容宽度而非无限拉伸图表。
- 交互目标最低 44×44px；相邻触摸目标留出足够间距。hover 能力不能作为断点判断。

### 模式

- **Creator**：MVP 只正式支持 Windows/macOS 桌面 Chrome/Edge。受支持桌面中的窄窗口可以收起侧栏；手机和平板不提供创作或参数保存入口，只能打开独立 Viewer 查看已发布故事，并提示改用受支持桌面环境进行编辑。
- **Viewer**：从手机到桌面都保持完整阅读、解密、筛选、二维回退和等价说明。
- **故事区块卡片**：在故事内部保留一个主结论、一个核心视觉和必要范围；它不是独立嵌入产品，不能把完整故事等比缩小塞入卡片。
- **超宽屏观看**：只放大留白和可用绘图区，不增加新数据密度或引入实时刷新；它仍是同一响应式故事，不是实时 BI 大屏模式。
- **PDF/PNG**：使用确定性静态关键帧，移除交互幻觉、模糊依赖和悬浮状态；内容不因分页切断标题与图表语义。

## Accessibility

- 发布快照必须通过 WCAG 2.2 AA 核心门槛；不存在“品牌优先”的绕过按钮。
- 普通文字对比度至少 4.5:1，大字号至少 3:1；关键图形、边界和焦点等非文本对象至少 3:1。交互边界必须使用 `control-border-*`，不能用低对比装饰性 `border-*` 代替。
- 所有交互支持键盘；焦点顺序遵循故事 DOM，焦点环清晰且不被阴影、Canvas 或 3D 遮挡。
- 图表提供可访问名称、短摘要和必要的数据表；Canvas 模式必须保留同等文字内容。
- 不以颜色、方向、动画或声音单独编码状态；错误、趋势和选中态至少有第二种视觉/文字信号。
- 支持 200% 缩放和文本放大；核心内容不重叠、不裁切。移动端正文不小于 14px，重要正文优先 16px。
- 尊重 `prefers-reduced-motion`、高对比偏好和系统字体回退；降级前后数据、文字与操作含义一致。
- 主题或用户品牌色不合格时自动选择更安全的衍生色，并在发布预览中明确展示实际结果。

## Brand Assets & Content Voice

### Logo

- 主标志由“DataPulse AI”字标与脉冲轨迹符号组成；符号是两到四个数据点被一条连续趋势线连接，不使用医疗心电图、机器人头或魔法棒。
- 小尺寸图标只保留简化脉冲符号；小于 32px 时使用单色，不依赖渐变细节。
- 图标最小 24px，完整横向字标建议不小于 120px。四周安全区至少等于符号中单个数据点的直径。
- 深色、浅色和单色版本必须同时提供；不得拉伸、倾斜、添加额外辉光或把用户品牌色替换进 DataPulse 官方标志。

### 图标与图像

- 图标表达动作和状态，不做装饰性图标墙。采用同一开源图标族并在构建时固定版本。
- 品牌背景使用抽象数据轨迹、点阵、网格和柔和体积光；不使用假图表截图、AI 大脑、机器人、廉价商务握手或不可解释的随机粒子。
- 若使用插画，保持几何、克制、以数据流为主题，并确保删除插画后任务仍然可完成。

### 简体中文语气

- 句子短、具体、主动。按钮使用“动词 + 对象”：`生成故事`、`应用时间范围`、`预览发送内容`、`创建分享链接`。
- 先说结果，再说依据和下一步。错误文案说明数据是否仍安全，不使用“发生未知错误，请稍后再试”作为唯一信息。
- 对 AI 使用“建议、组织、改写”，对确定性引擎使用“计算、验证、重算”。不要混称。
- 对安全和隐私准确限定范围：说“发布包在浏览器加密后上传”，不说“绝对安全”；说“DataPulse 云端的加密发布包和临时运行状态最长保留 24 小时；可选的无内容遥测按独立披露最多保留 30 天，达到最小样本阈值的汇总可长期保存”。第三方模型供应商的数据处理与保留政策必须单独说明，不能包含在 DataPulse 的时限承诺中。
- 避免夸张词：`震撼`、`惊人`、`颠覆`、`洞察一切`、`完全匿名`、`永久安全`。

推荐示例：

- “已按 2026 年 1–6 月范围更新 6 个指标和 3 条结论。”
- “该分组只有 4 条有效记录，暂不生成趋势结论。”
- “将向 Glosc AI 发送以下汇总证据；不包含原始行、直接标识、自由文本或机密字段。”

## Known Gaps

以下内容尚未由实现或真实用户/设备验证。Agent 不得自行把猜测固化为品牌规则：

- 最终 Logo 矢量资产、字标字距和商标注册尚未完成；当前只定义方向。
- `Space Grotesk`、`Inter`、`Noto Sans SC`/`Noto Serif SC` 的打包、中文子集、许可证清单和 PDF 嵌入仍需 M0/M3 验证。
- 用户品牌色的 OKLCH 派生算法、自动对比修正和图表色觉模拟尚需实现夹具。
- Creator 四区布局尚未经过真实汇报制作者可用性研究，断点可通过证据化设计评审调整。
- 四主题的完整组件矩阵、视觉基线和二维/3D 对照要到 M3 建立。
- 3D 材质、GPU 预算和 Safari/微信表现仍需真实设备认证；在此之前默认采用二维效果。
- 市场官网的摄影、插画和社区模板风格不在当前 MVP 品牌资产范围。

## Maintenance & Agent Prompt Guide

### 维护

- 改变颜色、字体、间距、圆角或公共组件时，同一 PR 更新 YAML Token、对应正文、Storybook 和视觉基线。
- CSS 变量、Tailwind/W3C Token、ECharts 主题和预览清单应从本文件生成或自动核对，不可独立手改为第二事实源。
- 使用 Google Stitch CLI 对本文件执行 lint；错误必须修复，warning 必须逐项审查。CLI 的对比度检查不能替代图表、键盘、非文本对比和真实浏览器无障碍测试。
- 有意视觉差异和基线在同一 PR 审查，禁止批量重录掩盖漂移。
- `version: alpha` 表示所采用的外部格式仍可能变化；升级 CLI/spec 前先核对 Schema、导出和 lint 行为。

### Agent 生成界面时的顺序

1. 判断正在设计产品壳、Creator、Viewer、故事主题、故事区块还是静态导出。
2. 从 YAML Token 和已注册组件开始，不从自创渐变或页面截图开始。
3. 先实现清晰二维内容、数据状态和直接编辑，再添加动效和 3D。
4. 补齐 loading、empty、partial、error、small-sample、protected 和 not-applicable 状态。
5. 验证四主题、桌面/平板/手机、键盘、触摸、弱动效和二维回退。
6. 对新增视觉模式说明“Token + 使用规则 + 理由 + 禁止场景”，并同步预览。

### 外部格式参考

- [getdesign.md](https://getdesign.md/)
- [Google Stitch DESIGN.md specification](https://stitch.withgoogle.com/docs/design-md/specification/)
- [本项目的 AGENTS.md / DESIGN.md 指南研究](docs/research/agents-and-design-guides.md)
